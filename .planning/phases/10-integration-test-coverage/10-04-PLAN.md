# Plan 10-04: AgentSelection + Voice Service Guards + Integration Sweep

## Objective
Test AgentSelectionViewModel (Stockfish lifecycle, download flow), voice service guard behavior (error paths that don't need WebSocket), and run a final integration sweep to verify the complete system.

## Prerequisites
- Plans 10-01 through 10-03 complete

## TDD Approach
Write failing tests first. Minor csproj change to include AgentSelectionViewModel for net8.0.

## Tasks

### Task 1: Enable AgentSelectionViewModel for net8.0
**File:** `src/GaimerDesktop/GaimerDesktop/GaimerDesktop.csproj`

Add selective re-include (alongside MainViewModel from Plan 10-03):
```xml
<Compile Include="ViewModels\AgentSelectionViewModel.cs" />
```

Resolve any compilation issues (Agent model dependency, etc.).

### Task 2: AgentSelectionViewModel Tests (TDD)
**File:** `src/GaimerDesktop/GaimerDesktop.Tests/ViewModels/AgentSelectionViewModelTests.cs`
**Production:** `src/GaimerDesktop/GaimerDesktop/ViewModels/AgentSelectionViewModel.cs`

Tests (~12):
1. `Constructor_InitializesAvailableAgents`
2. `AvailableAgents_ContainsLeroy`
3. `AvailableAgents_ContainsWasp`
4. `IsStockfishInstalled_DelegatesToService`
5. `SelectAgentAsync_NullId_DoesNothing`
6. `SelectAgentAsync_UnknownId_DoesNothing`
7. `SelectAgentAsync_ChessAgent_NoStockfish_ShowsDownloadOverlay`
8. `SelectAgentAsync_ChessAgent_StockfishInstalled_StartsEngine`
9. `DownloadStockfishAsync_NullService_IsNoop`
10. `DownloadStockfishAsync_Success_SetsIsDownloading`
11. `DownloadStockfishAsync_Failure_SetsDownloadError`
12. `SkipDownloadAsync_HidesOverlay`

Note: Navigation paths (Shell.Current) will be null-safe tested. VoiceGender path untestable without refactoring Application.Current service locator (accepted gap).

### Task 3: Voice Service Guard Tests (TDD)
**File:** `src/GaimerDesktop/GaimerDesktop.Tests/Conversation/VoiceServiceGuardTests.cs`

Test the error paths that DON'T require WebSocket connections. These are the constructors, null guards, and disconnected-state guards on GeminiLiveService and OpenAIRealtimeService.

**GeminiLiveService guards (~5):**
1. `Gemini_ConnectAsync_NullAgent_ThrowsArgumentNull`
2. `Gemini_ConnectAsync_EmptyApiKey_FiresErrorAndSetsErrorState`
3. `Gemini_SendAudioAsync_WhenDisconnected_ReturnsImmediately`
4. `Gemini_SendTextAsync_WhenDisconnected_ReturnsImmediately`
5. `Gemini_Dispose_IsIdempotent`

**OpenAIRealtimeService guards (~6):**
6. `OpenAI_ConnectAsync_NullAgent_ThrowsArgumentNull`
7. `OpenAI_ConnectAsync_EmptyApiKey_FiresErrorAndSetsErrorState`
8. `OpenAI_SendAudioAsync_WhenDisconnected_ReturnsImmediately`
9. `OpenAI_CommitAudioBuffer_WhenDisconnected_ReturnsImmediately`
10. `OpenAI_CancelResponse_WhenDisconnected_ReturnsImmediately`
11. `OpenAI_Dispose_IsIdempotent`

**Provider property tests (~4):**
12. `GeminiProvider_SupportsVideo_IsTrue`
13. `GeminiProvider_ProviderName_IsGeminiLive`
14. `OpenAIProvider_SupportsVideo_IsFalse`
15. `OpenAIProvider_ProviderName_IsOpenAIRealtime`

### Task 4: Integration Seam Verification
**File:** `src/GaimerDesktop/GaimerDesktop.Tests/Integration/PipelineIntegrationTests.cs`

End-to-end tests that verify critical integration seams work when components are wired together (not just individually mocked).

Tests (~5):
1. `FrameDiff_ToBrain_ToChannel_ToRouter_EndToEnd` — FrameDiffService.HasChanged → BrainService.SubmitImageAsync → Channel write → Router dispatches
2. `SessionTransition_ToolAvailability_Consistency` — SessionManager transitions match ToolExecutor.GetAvailableToolDefinitions
3. `BrainContext_L1Ingest_ToVoiceContext_Flow` — BrainContextService ingests event → GetContextForVoiceAsync returns it
4. `ProviderFactory_ToProvider_ConnectState_Cycle` — Factory creates MockProvider → ConnectAsync → State=Connected → DisconnectAsync → State=Disconnected
5. `StockfishLifecycle_AgentType_Consistency` — Agent.Type for chess agents → StockfishService.StartAsync path

### Task 5: Final test suite run + audit summary
Run: `dotnet test -f net8.0`

Expected total: 301 (existing) + ~31 (Plan 01) + ~25 (Plan 02) + ~32 (Plan 03) + ~42 (Plan 04) ≈ **~430 tests**

Update:
- `.planning/STATE.md` with final test count
- Memory MEMORY.md with test infrastructure notes

## Success Criteria
- [ ] AgentSelectionViewModel compiles and tested under net8.0
- [ ] Voice service guard behavior verified (~15 tests)
- [ ] Critical integration seams exercised (~5 tests)
- [ ] ~42 new tests, all passing
- [ ] Full suite green (~430 total)
- [ ] Audit gap analysis shows significant improvement

## Estimated Tests: ~42
