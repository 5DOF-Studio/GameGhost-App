# Plan 09-03: Agent System Prompt + Chess Skill Download UX

## Goal
Update Leroy and Wasp system instructions to guide dual-tool usage (engine vs strategic analysis). Build the "Download Chess Skills" UX flow triggered on first chess agent selection. Wire Stockfish lifecycle to agent selection/deselection.

## Dependencies
- 09-01 (StockfishService + Downloader)
- 09-02 (Dual tools registered)

## Files to Modify

### `Models/Agent.cs` — Update Leroy + Wasp system instructions

Both chess agents get identical tool guidance appended to their existing personality prompts:

```
CHESS TOOLS:
You have two analysis tools. Choose based on the situation:

1. analyze_position_engine — Stockfish chess engine (AUTHORITATIVE)
   Use when: player asks "what's the best move", tactical complications exist,
   you need to verify a candidate move, or the position is sharp/forcing.
   Requires: extract FEN from the screenshot first.
   Returns: best move, evaluation, top 3 candidate moves with continuations.

2. analyze_position_strategic — Strategic analysis
   Use when: player asks "what's the plan", position is quiet/strategic,
   you want to explain themes/ideas, or teach positional concepts.
   Returns: strategic themes, piece activity, pawn structure, plans.

COMBINED USAGE (critical moments):
For important positions, call analyze_position_engine FIRST to get the objective truth,
then synthesize your explanation incorporating both engine data and strategic understanding.
Say things like "The engine recommends knight to f3 — this makes sense because it
develops with tempo and prepares castling."

FEN EXTRACTION:
When calling analyze_position_engine, you must provide the FEN string.
Extract it carefully from the screenshot:
- Count pieces rank by rank from Black's back rank (rank 8) to White's (rank 1)
- Use: K=king Q=queen R=rook B=bishop N=knight P=pawn (uppercase=white, lowercase=black)
- Empty squares = digit (consecutive empties sum to one number)
- Separate ranks with /
- After the board: space, side to move (w/b), space, castling rights (KQkq or -), space, en passant square (or -), space, halfmove clock, space, fullmove number
If you're unsure about any piece, state your uncertainty and provide your best attempt.

VOICE OUTPUT:
- Say "play knight to f3" not "Nf3"
- Say "bishop takes f7, check" not "Bxf7+"
- Say "castle kingside" not "O-O"
- Translate evaluations: +0.3 to +0.8 = "slight edge", +0.8 to +2.0 = "clear advantage"
- Keep responses under 3 sentences for routine positions
- Expand to 4-5 sentences for critical moments
- Never read raw centipawn values or engine lines to the user
```

Update `Agent.Tools` list for both Leroy and Wasp:
```csharp
Tools = ["capture_screen", "analyze_position_engine", "analyze_position_strategic", "get_game_state", "web_search"]
```

### `Views/AgentSelectionPage.xaml` — Chess Skill Download UI

When user taps SELECT on a chess agent and Stockfish isn't installed, show a download overlay:

**Design (dark theme, consistent with existing cards):**
```
╔══════════════════════════════════════╗
║  CHESS SKILLS                        ║
║                                      ║
║  [Chess piece icon]                  ║
║                                      ║
║  Download the Stockfish chess engine  ║
║  for position analysis and move      ║
║  recommendations.                    ║
║                                      ║
║  Size: ~75 MB                        ║
║  Powered by: Stockfish 18 (GPLv3)   ║
║                                      ║
║  ████████████░░░░░░░░  45%           ║
║                                      ║
║  [DOWNLOAD]        [SKIP FOR NOW]    ║
╚══════════════════════════════════════╝
```

- **DOWNLOAD**: starts download with progress bar, then navigates to MainPage
- **SKIP FOR NOW**: navigates to MainPage without Stockfish (falls back to Lichess cloud eval)
- After download completes: auto-navigate to MainPage
- If already installed: skip this screen entirely, go straight to MainPage

### `Views/AgentSelectionPage.xaml.cs` — Download flow logic

Modify `OnLeroySelectClicked` and `OnWaspSelectClicked`:
1. Check `IStockfishService.IsInstalled`
2. If installed: proceed to MainPage (existing flow)
3. If not installed: show download overlay (XAML element toggled visible)
4. DOWNLOAD button: call `IStockfishService.EnsureInstalledAsync(progressReporter)`, update progress bar
5. On complete: hide overlay, navigate to MainPage
6. SKIP FOR NOW: navigate directly (Lichess fallback works)

### `ViewModels/AgentSelectionViewModel.cs` — Stockfish lifecycle

- Inject `IStockfishService` via DI
- In `SelectAgentAsync`: if chess agent selected AND Stockfish is installed, call `_stockfishService.StartAsync()`
- Expose `IsStockfishInstalled` property for UI binding
- Expose `DownloadProgress` property (0.0-1.0) for progress bar

### `ViewModels/MainViewModel.cs` — Engine lifecycle on disconnect

When user disconnects or switches agent:
- Call `_stockfishService.StopAsync()` to gracefully shutdown the engine process
- Engine restarts when user selects a chess agent again

### `MauiProgram.cs` — Wire Stockfish into DI

```csharp
// Stockfish engine
builder.Services.AddSingleton<IStockfishService>(sp =>
{
    if (useMockServices)
        return new MockStockfishService();
    return new StockfishService();
});
```

Pass `IStockfishService` to `AgentSelectionViewModel` constructor.

## Tests to Write (TDD)

### `Tests/Chess/AgentSystemPromptTests.cs`
- Leroy system instruction contains "analyze_position_engine"
- Leroy system instruction contains "FEN EXTRACTION" guidance
- Wasp system instruction contains same tool guidance
- Both agents' Tools list includes "analyze_position_engine" and "analyze_position_strategic"

### `Tests/Chess/StockfishLifecycleTests.cs`
- Selecting chess agent when installed: StartAsync called
- Selecting chess agent when not installed: StartAsync NOT called
- Disconnecting: StopAsync called
- Selecting non-chess agent: StopAsync called (or never started)
- Mock download: EnsureInstalledAsync reports progress 0.0 -> 1.0

### `Tests/Chess/DownloadFlowTests.cs`
- Already installed: download UI not shown, navigate directly
- Not installed, user clicks download: progress reported, then navigate
- Not installed, user clicks skip: navigate without download
- Download failure: show error, offer retry

## Implementation Order
1. Write agent prompt tests -> update Agent.cs system instructions + Tools list
2. Write lifecycle tests -> implement ViewModel Stockfish lifecycle
3. Build download overlay XAML
4. Wire download button handlers
5. Update DI registrations
6. Verify full flow: select agent -> download prompt -> download -> navigate -> engine starts

## UX Notes
- The "Chess Skills" framing makes Stockfish feel like an agent capability, not a dependency
- Users who skip still get coaching (Lichess fallback + LLM strategic analysis)
- Progress bar uses existing gamer theme (purple/cyan accents)
- Download happens once, persists across sessions
- No mention of "Stockfish" or "GPL" in the main UI — that's in the about/settings page
