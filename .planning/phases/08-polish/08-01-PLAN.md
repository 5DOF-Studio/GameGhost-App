---
phase: 08-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/GaimerDesktop/GaimerDesktop/ViewModels/MainViewModel.cs
  - src/GaimerDesktop/GaimerDesktop/Services/Conversation/Providers/GeminiConversationProvider.cs
  - src/GaimerDesktop/GaimerDesktop/Services/Conversation/Providers/OpenAIConversationProvider.cs
  - src/GaimerDesktop/GaimerDesktop.Tests/Integration/PipelineIntegrationTests.cs
  - src/GaimerDesktop/GaimerDesktop.Tests/Services/Auth/SupabaseAuthServiceTests.cs
autonomous: true

must_haves:
  truths:
    - "No Console.WriteLine calls remain in MainViewModel (all replaced with Debug.WriteLine)"
    - "GeminiConversationProvider and OpenAIConversationProvider correctly report Disconnecting state through State property"
    - "PipelineIntegrationTests use polling loop instead of fixed Task.Delay(1500)"
    - "SupabaseAuthServiceTests use URL inspection instead of call-counter pattern"
    - "All 488+ existing tests still pass"
  artifacts:
    - path: "src/GaimerDesktop/GaimerDesktop/ViewModels/MainViewModel.cs"
      provides: "Clean logging (no Console.WriteLine)"
      contains: "Debug.WriteLine"
    - path: "src/GaimerDesktop/GaimerDesktop/Services/Conversation/Providers/GeminiConversationProvider.cs"
      provides: "Correct Disconnecting state delegation"
    - path: "src/GaimerDesktop/GaimerDesktop/Services/Conversation/Providers/OpenAIConversationProvider.cs"
      provides: "Correct Disconnecting state delegation"
    - path: "src/GaimerDesktop/GaimerDesktop.Tests/Integration/PipelineIntegrationTests.cs"
      provides: "Polling-based wait instead of fixed delay"
    - path: "src/GaimerDesktop/GaimerDesktop.Tests/Services/Auth/SupabaseAuthServiceTests.cs"
      provides: "URL-based request verification"
  key_links:
    - from: "GeminiConversationProvider.DisconnectAsync"
      to: "State property"
      via: "State = ConnectionState.Disconnecting before inner service call"
      pattern: "State = ConnectionState\\.Disconnecting"
---

<objective>
Fix 5 remaining code review issues from the Phase 10 audit: Console.WriteLine cleanup in MainViewModel, Disconnecting state delegation in conversation providers, PipelineIntegrationTests flaky delay, and SupabaseAuthServiceTests call-counter pattern.

Purpose: Stabilize the foundation before higher-risk Voice Chat and capture parameterization changes. These are low-risk, well-understood fixes.
Output: Cleaner logging, correct state transitions, deterministic tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-polish/08-RESEARCH.md
@src/GaimerDesktop/GaimerDesktop/ViewModels/MainViewModel.cs
@src/GaimerDesktop/GaimerDesktop/Services/Conversation/Providers/GeminiConversationProvider.cs
@src/GaimerDesktop/GaimerDesktop/Services/Conversation/Providers/OpenAIConversationProvider.cs
@src/GaimerDesktop/GaimerDesktop.Tests/Integration/PipelineIntegrationTests.cs
@src/GaimerDesktop/GaimerDesktop.Tests/Services/Auth/SupabaseAuthServiceTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Console.WriteLine cleanup + Disconnecting state fix</name>
  <files>
    src/GaimerDesktop/GaimerDesktop/ViewModels/MainViewModel.cs
    src/GaimerDesktop/GaimerDesktop/Services/Conversation/Providers/GeminiConversationProvider.cs
    src/GaimerDesktop/GaimerDesktop/Services/Conversation/Providers/OpenAIConversationProvider.cs
  </files>
  <action>
    **MainViewModel.cs Console.WriteLine cleanup (5 locations):**
    - Line 206: `Console.WriteLine($"[Audio] {e.Message} {e.Exception}")` — replace with `System.Diagnostics.Debug.WriteLine($"[Audio] {e.Message} {e.Exception}")`
    - Line ~296: `Console.WriteLine($"[GhostMode] ExitGhostModeAsync on disconnect failed: ...")` — replace with `System.Diagnostics.Debug.WriteLine`
    - Line ~401: `Console.WriteLine($"[GhostMode] ToggleFab failed: {ex.Message}")` — replace with `System.Diagnostics.Debug.WriteLine`
    - Line ~413: `Console.WriteLine("[GhostMode] Gear tapped — audio card shown natively")` — replace with `System.Diagnostics.Debug.WriteLine`
    - Line ~631: `Console.WriteLine("[Gaimer] ConnectAsync returned but IGeminiService is not connected...")` — replace with `System.Diagnostics.Debug.WriteLine`

    Do a full scan for any remaining `Console.WriteLine` in MainViewModel.cs after these replacements. There should be zero.

    **Disconnecting state delegation fix:**
    Both GeminiConversationProvider and OpenAIConversationProvider fire `ConnectionStateChanged?.Invoke(this, ConnectionState.Disconnecting)` in their DisconnectAsync methods, but they do NOT update their own `State` property to `Disconnecting` first. The MockConversationProvider does this correctly (line 56: `State = ConnectionState.Disconnecting`).

    Fix in GeminiConversationProvider.DisconnectAsync():
    - Before the `ConnectionStateChanged?.Invoke(this, ConnectionState.Disconnecting)` line, add: `State = ConnectionState.Disconnecting;`
    - Pattern: match what MockConversationProvider does (set State, then invoke event)

    Fix in OpenAIConversationProvider.DisconnectAsync():
    - Same fix: add `State = ConnectionState.Disconnecting;` before the event invocation line

    Check if both providers also need to update State to Disconnected after the inner service disconnect completes. Look at what MockConversationProvider does for the full disconnect flow and mirror it.
  </action>
  <verify>
    - `grep -rn "Console.WriteLine" src/GaimerDesktop/GaimerDesktop/ViewModels/MainViewModel.cs` returns no results
    - `grep -n "State = ConnectionState.Disconnecting" src/GaimerDesktop/GaimerDesktop/Services/Conversation/Providers/GeminiConversationProvider.cs` returns a match
    - `grep -n "State = ConnectionState.Disconnecting" src/GaimerDesktop/GaimerDesktop/Services/Conversation/Providers/OpenAIConversationProvider.cs` returns a match
    - `dotnet build src/GaimerDesktop/GaimerDesktop/GaimerDesktop.csproj -f net8.0-maccatalyst -p:EnableCodeSigning=false` succeeds
  </verify>
  <done>Zero Console.WriteLine in MainViewModel. Both conversation providers correctly set State = Disconnecting before firing event, matching MockConversationProvider behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Fix flaky PipelineIntegrationTests + SupabaseAuthService test patterns</name>
  <files>
    src/GaimerDesktop/GaimerDesktop.Tests/Integration/PipelineIntegrationTests.cs
    src/GaimerDesktop/GaimerDesktop.Tests/Services/Auth/SupabaseAuthServiceTests.cs
  </files>
  <action>
    **PipelineIntegrationTests.cs line 59:**
    Replace `await Task.Delay(1500);` with a polling loop that checks for the expected condition (brain result processed, timeline updated, etc.). Pattern:
    ```csharp
    // Poll for up to 3 seconds (generous ceiling)
    var deadline = DateTime.UtcNow.AddSeconds(3);
    while (DateTime.UtcNow < deadline)
    {
        if (/* condition that signals completion */) break;
        await Task.Delay(50);
    }
    ```
    Read the full test to understand what condition it's waiting for (likely a brain result appearing on the channel or router processing a result). The polling condition should check whatever the assertion after the delay checks.

    **SupabaseAuthServiceTests.cs (lines 184-188, 226-230):**
    The tests use a `callCount` variable incremented in the mock HTTP handler to distinguish first vs second calls. This is fragile. Replace with URL path inspection:
    ```csharp
    // Instead of:
    // callCount++;
    // if (callCount == 1) ...

    // Use:
    // if (request.RequestUri?.PathAndQuery.Contains("/expected-path") == true) ...
    ```
    Read the full test methods to understand the actual HTTP endpoints being called (likely Supabase auth endpoints like `/auth/v1/token`, `/auth/v1/signup`, etc.) and use the URL path to distinguish the responses.
  </action>
  <verify>
    - `dotnet test src/GaimerDesktop/GaimerDesktop.Tests/ -f net8.0 --filter "PipelineIntegration"` passes
    - `dotnet test src/GaimerDesktop/GaimerDesktop.Tests/ -f net8.0 --filter "SupabaseAuth"` passes
    - `dotnet test src/GaimerDesktop/GaimerDesktop.Tests/ -f net8.0` — all 488+ tests pass
    - `grep -n "Task.Delay(1500)" src/GaimerDesktop/GaimerDesktop.Tests/Integration/PipelineIntegrationTests.cs` returns no results
    - `grep -n "callCount" src/GaimerDesktop/GaimerDesktop.Tests/Services/Auth/SupabaseAuthServiceTests.cs` returns no results
  </verify>
  <done>PipelineIntegrationTests use deterministic polling. SupabaseAuthServiceTests use URL inspection for request routing. Full test suite passes.</done>
</task>

</tasks>

<verification>
- `dotnet build src/GaimerDesktop/GaimerDesktop/GaimerDesktop.csproj -f net8.0-maccatalyst -p:EnableCodeSigning=false` — clean build
- `dotnet test src/GaimerDesktop/GaimerDesktop.Tests/ -f net8.0` — all tests pass (488+)
- Zero `Console.WriteLine` in MainViewModel.cs
- Both real conversation providers mirror MockConversationProvider's Disconnecting state pattern
</verification>

<success_criteria>
All 5 remaining code review items resolved. Test suite green. No regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/08-polish/08-01-SUMMARY.md`
</output>
